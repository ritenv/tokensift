---
"tokensift": minor
---

Add cache-buster rule: flags dynamic content placed before a large static block, which blocks that block from ever being cached under provider prompt caching's exact-prefix matching
